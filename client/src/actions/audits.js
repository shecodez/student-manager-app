/* import { auditLog } from '../firebase';

export function createLog(userRef, operation, operationRef, table) {
  const log = {
    userRef,
    operation,
    operationRef,
    table,
    timestamp: Date.now();
  }
  return dispatch => auditLog.ref('audits').push(log) 
} */
